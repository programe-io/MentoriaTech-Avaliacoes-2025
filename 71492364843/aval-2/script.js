// Seleciona o botão pelo ID
const botao = document.getElementById('btnMensagem');

// Adiciona evento de clique
botao.addEventListener('click', function() {
    alert('Bem-vindo à Academia Kleuton! Vamos treinar juntos!');
});
