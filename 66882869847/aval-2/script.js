// Script simples para exemplo
document.addEventListener("DOMContentLoaded", () => {
  console.log("Site do Isaac carregado com sucesso!");

    // Exemplo: clicar no título mostra uma mensagem
      const titulo = document.querySelector("h1");
        titulo.addEventListener("click", () => {
            alert("Bem-vindo ao Site do Isaac!");
              });
              });