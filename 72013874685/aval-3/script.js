/**
 * Arquivo: script.js
 * Descrição: Lógica interativa da página "Garagem Clássica".
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Garagem Clássica - O script está carregado e pronto!');

    // Exemplo de código futuro: Adicionar um listener para um botão "curtir"
    /*
    const likeButtons = document.querySelectorAll('.like-button');
    likeButtons.forEach(button => {
        button.addEventListener('click', () => {
            alert('Você curtiu este clássico!');
        });
    });
    */

    // Exemplo de código futuro: Adicionar modo escuro.
    /*
    const darkModeToggle = document.getElementById('dark-mode-toggle');
    darkModeToggle.addEventListener('click', () => {
        document.body.classList.toggle('dark');
    });
    */
});