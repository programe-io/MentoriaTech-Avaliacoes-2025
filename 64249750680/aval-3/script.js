// Placeholder de funcionalidade mobile
document.addEventListener("DOMContentLoaded", () => {
    console.log("Site de automobilismo carregado.");

    const menuBtn = document.getElementById("menu-btn");
    menuBtn?.addEventListener("click", () => {
        alert("Menu mobile ainda não implementado!");
    });
});
