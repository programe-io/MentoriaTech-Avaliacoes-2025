function jogar(nomeJogo) {
  alert("Você escolheu jogar: " + nomeJogo);
  // Aqui você pode redirecionar para uma página do jogo, por exemplo:
  // window.location.href = 'jogos/' + nomeJogo.toLowerCase() + '.html';
}
