// Arquivo: script.js
// Aqui é onde você adicionaria a lógica interativa da página,
// como manipulação de eventos (cliques), requisições de dados, etc.

document.addEventListener('DOMContentLoaded', () => {
    console.log('A página Social Feed foi carregada com sucesso!');
    
    // Exemplo de função que você adicionaria:
    // const likeButtons = document.querySelectorAll('.like-button');
    // likeButtons.forEach(button => {
    //     button.addEventListener('click', handleLike);
    // });
});

// function handleLike(event) {
//    // Lógica para curtir um post
//    alert('Post curtido!');
// }