// Seleciona o botão
const botao = document.getElementById("meuBotao");

// Quando clicar no botão → aparece mensagem
botao.addEventListener("click", function() {
    alert("Você clicou no botão! 🎉");
});
