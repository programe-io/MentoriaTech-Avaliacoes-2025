function mostrarMensagem() {
      alert("Bem-vindo ao SITE do IMPERADOR 🚀✈️!");
      }

      /* Função de exemplo para o botão de publicar (evita redirecionamento) */
      function publicarGitHub(event) {
        event.preventDefault();
          // Aqui só mostramos um alerta; se quiser, podemos abrir uma URL real do GitHub Pages
            alert("Ação: publicar no GitHub Pages (funcionalidade de exemplo).");
            }
}